first.className = "text-dark red" ;

//To remove the class red from the classes
first.classList.remove("red");

//To add the class "red" to existing class
first.classList.add("red") ;

//toggle - If class already exists , remove it . If not present , add it .
// first.classList.toggle("red") ;


//To check if the element has class or not
first.classList.contains("red") ;

